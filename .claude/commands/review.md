Screenshot every route at 1440, 1024, 768, and 390 (use `npm run shots` or take them directly). Compare each screenshot against the tokens declared in `app/globals.css` — colors, type scale, spacing unit, radius.

List violations only: what's off-spec, and where. No praise, no summary, no "looks good."
